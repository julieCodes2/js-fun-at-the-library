function shelfBook(title, shelf) {
  if (shelf.length < 3) {
    //if the length of the array is less than 3 then the next line is saying put the title in the array
    shelf.unshift(title);

  }
  //function- what is the purpose of this function- to shelf books
  //need parameters
//console.log(title);
//console.log(shelf)

}
function unshelfBook(title, shelf) {
for (var i = 0; i > 3; i--)
title.
}
//break
module.exports = {
  shelfBook,
  unshelfBook,
  // listTitles,
  // searchShelf
};
